spring:
  datasource:
    url: jdbc:mysql://localhost:3306/vshm
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true

app:
  jwt:
    secret: change-me-to-a-long-random-secret-key-change-me
    expiration-ms: 86400000
